name: CI

on: [ push, pull_request ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - uses: ./.github/actions/set-env-action
      with:
        envs:
          test1: "value1"
          test2: "value1"
